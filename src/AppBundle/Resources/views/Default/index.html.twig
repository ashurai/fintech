{% extends "AppBundle::layout.html.twig" %}
{%block body %}
<div class="be-content">
        <div class="page-head">
          <h2 class="page-head-title">Expenses</h2>
          <ol class="breadcrumb page-head-nav">
            <li><a href="{{path('homepage')}}">Home</a></li>
          </ol>
        </div>
          {% include "AppBundle::flashMessage.html.twig" %}
        <div class="main-content container-fluid">
          <div class="row">
            <div class="col-sm-12">
              
              <div class="panel panel-default panel-border-color panel-border-color-primary">
                <div class="panel-heading panel-heading-divider">List All data
                    <p class="text-right">
                        <a href="{{path('new')}}" class="btn btn-space btn-primary">Add New</a>
                    </p>
                    <span class="panel-subtitle">This is basic ajax tale, we can replace with datatable with common helper </span></div>
                <div class="panel-body">
                  <table id="table3" class="table table-striped table-hover table-fw-widget">
                    <thead>
                      <tr>
                        <th>No</th>
                        <th>Description</th>
                        <th>Total Amount</th>
                        <th>Action</th>
                      </tr>
                    </thead>
                    <tbody id="tableData">                      
                    </tbody>
                  </table>
                </div>
              </div>
            </div>            
          </div>
        </div>
      </div>
{% endblock body%}

    